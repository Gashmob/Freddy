{
  "name": "freddy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && vite build",
    "watch": "vite build --watch --mode development --minify false",
    "test": "vitest",
    "prettier": "prettier"
  },
  "devDependencies": {
    "prettier": "2.8.8",
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  }
}
